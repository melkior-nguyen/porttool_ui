import { enUS } from "@mui/material/locale";
import enMessages from "./en_US.json";

const EnLang = {
  messages: {
    ...enMessages,
  },
  muiLocale: enUS,
  locale: "en-US",
};
export default EnLang;
